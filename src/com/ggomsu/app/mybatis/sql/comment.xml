<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Comment">
	<select id ="getCommentList" parameterType="_int" resultType="comment">
		SELECT * FROM comments
		WHERE articleIndex = #{articleIndex}
		ORDER BY refIndex ASC, commentIndex ASC
	</select>
	<insert id ="insertComment" parameterType="comment">
		INSERT INTO comments(articleIndex, commentIndex, refIndex, nickname, content)
		VALUES(#{articleIndex}, #{commentIndex}, #{refIndex}, #{nickname}, #{content})
	</insert>
	<insert id ="insertRefComment" parameterType="comment">
		INSERT INTO comments(articleIndex, commentIndex, refIndex, nickname, content)
		VALUES(#{articleIndex}, #{commentIndex}, #{refIndex}, #{nickname}, #{content})
	</insert>
	<delete id="deleteComment" parameterType="comment">
		DELETE FROM comments
		WHERE articleIndex=${articleIndex} AND commentIndex=${commentIndex} AND refIndex=${refIndex}
	</delete>
	<select id="getCommentIndex" resultType="_int">
		SELECT `commentIndex` FROM comments 
		ORDER BY `commentIndex` DESC
		LIMIT 1
	</select>
</mapper>
