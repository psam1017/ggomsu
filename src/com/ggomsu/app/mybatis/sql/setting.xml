<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<!-- 작성자 : 손하늘 -->
<mapper namespace="Setting">

	<!-- 시스템 셋팅 -->
	<select id="selectSetting" parameterType="string" resultType="setting">
		SELECT articleAlarmFlag, commentAlarmFlag, darkModeFlag
		FROM members
		WHERE email = #{email}
	</select>
	<update id="updateSetting" parameterType="setting"> 
	 	UPDATE members 
	  	SET 
	  	articleAlarmFlag = ${articleAlarmFlag}, commentAlarmFlag = ${commentAlarmFlag}, darkModeFlag = ${darkModeFlag}
	  	WHERE email = #{email}
	</update>
	
</mapper>